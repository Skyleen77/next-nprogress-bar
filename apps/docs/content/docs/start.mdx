---
title: start()
description: BProgress.start() method
---

The `start()` method is used to start the progress bar.

## Usage

```js
BProgress.start();
```

## Typing

```ts
BProgress.start(): BProgress;
```
