---
title: isStarted()
description: BProgress.isStarted() method
---

The `isStarted()` method is used to check if the progress bar is started.

## Usage

```js
BProgress.isStarted();
```

## Typing

```ts
BProgress.isStarted(): boolean;
```
