---
title: resume()
description: BProgress.resume() method
---

The `resume()` method is used to resume the progress bar after it has been paused.

## Usage

```js
BProgress.resume();
```

## Typing

```ts
BProgress.resume(): BProgress;
```
