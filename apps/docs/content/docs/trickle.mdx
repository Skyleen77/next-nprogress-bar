---
title: trickle()
description: BProgress.trickle() method
---

The `trickle()` method increments the progress bar by a small amount, giving the impression of gradual progress.

## Usage

```js
BProgress.trickle();
```

## Typing

```ts
BProgress.trickle(): BProgress;
```
