---
title: isRendered()
description: BProgress.isRendered() method
---

The `isRendered()` method is used to check if the progress bar is rendered.

## Usage

```js
BProgress.isRendered();
```

## Typing

```ts
BProgress.isRendered(): boolean;
```
